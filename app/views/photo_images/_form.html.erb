<%= form_for(@photo_image) do |f| %>
  <% if @photo_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_image.errors.count, "error") %> prohibited this photo_image from being saved:</h2>

      <ul>
      <% @photo_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
